🇮🇷 فارسی

در این پوشه مراحل مربوط به وارد کردن داده‌ها (Import) به SQL Server توضیح داده می‌شود.
هدف این مرحله، انتقال فایل‌های CSV به جدول‌های دیتابیس است.

در این مرحله:

داده‌ها از فایل‌های CSV وارد SQL Server می‌شوند

روش‌های Import (Wizard یا دستورات SQL) مستند می‌شوند

در این مرحله هنوز هیچ تحلیل یا گزارشی انجام نمی‌شود.

🇬🇧 English

This folder documents the data import process into SQL Server.
The goal of this stage is to load CSV files into database tables.

At this stage:

Data is imported from CSV files into SQL Server

Import methods (Wizard or SQL commands) are documented

No analysis or reporting is performed at this stage.

🇩🇪 Deutsch

Dieser Ordner beschreibt den Import der Daten in SQL Server.
Ziel dieses Schrittes ist die Übertragung der CSV-Dateien in die Datenbanktabellen.

In diesem Schritt:

Daten werden aus CSV-Dateien in SQL Server importiert

Import-Methoden (Wizard oder SQL-Befehle) werden dokumentiert

In diesem Schritt werden noch keine Analysen oder Berichte erstellt.
